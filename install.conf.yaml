- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.bashrc:
    ~/.profile:
    ~/.gitconfig:
    ~/.dircolors: dircolors-solarized/dircolors.256dark
    ~/.xinitrc:
    ~/.Xresources:
    ~/.config/awesome: config/awesome
